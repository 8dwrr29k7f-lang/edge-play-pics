# Redeploy required

Your Discord still shows the OLD message:
"PASS the lock board until OpticOdds returns LOCK/CAP"

That code is deleted. After redeploy you should see desk LOCKS / LOTD instead.

1. Railway → Redeploy
2. Logs must include:
   - Overlay: overlay_liveEmbeds.js
   - Forced /locks → desk locksTodayEmbed
3. In Discord: /daily then /locks then /lotd

You do NOT need OpticOdds for named picks.
